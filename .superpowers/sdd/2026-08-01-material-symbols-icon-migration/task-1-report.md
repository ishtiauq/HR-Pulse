# Task 1 Report — Font package + `.msr` CSS + `Icon` component

## 1. What changed per file

- **`package.json`** — added dependency `@fontsource-variable/material-symbols-rounded` (`^5.3.1`) via `npm install`. The file also carries pre-existing unrelated dependency changes (@react-leaflet/core, framer-motion) that were already tracked from earlier work; they are included in this commit as expected.
- **`package-lock.json`** — regenerated by `npm install`; records the new font package (plus the pre-existing unrelated entries).
- **`src/index.css`** — added `@import "@fontsource-variable/material-symbols-rounded";` immediately after the existing Inter import (was line 4, now line 5), and appended the `.msr` class at the end of the file verbatim from the brief (font-family, weight 600, FILL 1, `display: inline-block`, ligatures, etc.).
- **`src/components/ui/Icon.jsx`** — **created** with the exact JSX from the brief: default-export `Icon({ name, size = 20, className, style, ariaLabel, ...props })`, renders a `<span className={cn("msr", className)}>` with `fontSize: size`, conditional `aria-hidden`/`aria-label`. Imports `cn` as a named export from `@/lib/utils` (verified in place: `export function cn(...inputs)` in `src/lib/utils.js:4`).

## 2. Build output (last ~15 lines)

```
dist/assets/material-symbols-rounded-latin-wght-normal-BPlF0bBf.woff2    959.77 kB
dist/assets/index-Clyqzjdx.css                                           172.67 kB │ gzip:  31.10 kB
dist/assets/purify.es-DuRL7t6i.js                                         26.87 kB │ gzip:  10.45 kB
dist/assets/index.es-Bb_n7HYl.js                                         151.32 kB │ gzip:  48.88 kB
dist/assets/html2canvas-D2s3HKpg.js                                      199.55 kB │ gzip:  46.77 kB
dist/assets/index-DIhIF_-8.js                                          2,059.60 kB │ gzip: 601.46 kB

✓ built in 870ms
[plugin builtin:vite-reporter]
(!) Some chunks are larger than 500 kB after minification. Consider:
- Using dynamic import() to code-split the application
- Use build.rolldownOptions.output.codeSplitting to improve chunking: https://rolldown.rs/reference/OutputOptions.codeSplitting
- Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
```

Build succeeded (`✓ built in 870ms`). The Material Symbols variable font (rounded, latin, wght-normal) was emitted to `dist/assets`. The only warning is a pre-existing large-chunk-size notice about the main `index` bundle (2 MB) — unrelated to this task. `Icon.jsx` is not imported anywhere yet, as the brief anticipated, so this proves the CSS/font load only.

## 3. Commit SHA

`2358d750334c622237a03bb03621a8b6d636ed1d`

- `feat: add Material Symbols variable font and Icon component`
- 4 files changed, 49 insertions: `package.json`, `package-lock.json`, `src/index.css`, `src/components/ui/Icon.jsx` (new).
- Verified via `git show --stat HEAD`; nothing unrelated staged. Untracked `.superpowers/sdd/...` and the plan doc remain unstaged (not part of this task).

## 4. Self-review notes / concerns

- Font weight: the emitted font asset is `material-symbols-rounded-latin-wght-normal`; the `.msr` class sets wght 600 via `font-variation-settings`, which is valid for a variable font — fine.
- The `dist` font is ~960 kB (large, single-latin subset) but is only loaded when CSS references `.msr` / the font-family, and later tasks will drive adoption. Worth revisiting subsetting/code-splitting during a performance pass, but not a blocker for this foundation task.
- No comments were added to code; conventions followed (`src/index.css` keeps Tailwind v4 `@import` syntax).
- Commit includes pre-existing unrelated package.json/lock changes as the task instructed; they were already tracked, not introduced by me.
